import React from 'react';

const Header: React.FC = () => {
  return (
    <header>
      {/* nav links */}
    </header>
  );
}

export default Header;

/*  Prof already created the navbar component so he'll just replace this with what he created */